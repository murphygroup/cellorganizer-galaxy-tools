<tool id="tcell_compare_models" name="Compare" version="0.0.3">
  <description> T cell models.</description>
  <command>
    matlab -r "diary diary.txt;
    tic;
    current_directory = pwd; 
    cellorganizer_directory = getenv('CELLORGANIZER'); 
    cd( cellorganizer_directory ); 
    setup(); 
    cd( current_directory );
    check_if_files_exist_on_disk_and_link_them_mat('$dataset');
    CompareTcellModels( './model00001.mat', './model00002.mat' );
    print(gcf,'output','-dpng');
    toc,
    diary off;
    exit;";
  </command>
  <inputs>
<param name="dataset" type="data" label="Choose a list of models" multiple="true" optional="false" />
  </inputs>
  <outputs>
  <data format="png" name="output" from_work_dir="output.png"/>
</outputs>
<help>

**Description**

Builds a plot that shows the comparison between two T cell models.

-----

**Input**

* A list composed of two T cell models

**Output**

* A PNG file

</help>
</tool>